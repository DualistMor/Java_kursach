# Loco yaml export: Symfony YAML object
# Project: service-registry.properties conversion
# Release: Working copy
# Locale: uk-UA, Ukrainian (Ukraine)
# Exported by: Unregistered user
# Exported at: Thu, 20 Jun 2019 14:56:18 +0300
---
server:
  port: "8063"
eureka:
  client:
    registerWithEureka: "false"
    fetchRegistry: "false"
    serviceUrl:
      defaultZone: http://localhost:8063/eureka/
  server:
    enable-self-preservation: "false"
spring:
  application:
    name: service-registry
...
