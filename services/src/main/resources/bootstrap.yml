# Loco yaml export: Symfony YAML object
# Project: services.properties conversion
# Release: Working copy
# Locale: uk-UA, Ukrainian (Ukraine)
# Exported by: Unregistered user
# Exported at: Thu, 20 Jun 2019 14:56:37 +0300
---
  spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
server:
  port: "8080"
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
  application:
    name: services
  rabbitmq:
    host: localhost
    port: "5672"
    username: guest
    password: guest
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8063/eureka/
prop:
  user:
    defaultname: sdfff
...
